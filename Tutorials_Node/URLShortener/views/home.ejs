<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shorten.io | Premium Link Management</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg-dark: #050505;
            --card-bg: #0f0f0f;
            --border-color: rgba(255, 255, 255, 0.08);
            --border-hover: rgba(255, 255, 255, 0.2);
            --accent: #fff;
            --text-main: #ededed;
            --text-muted: #888;
            --glow: conic-gradient(from 180deg at 50% 50%, #2a8af6 0deg, #a853ba 180deg, #e92a67 360deg);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            background-image: radial-gradient(circle at 50% 0%, #1a1a1a 0%, var(--bg-dark) 70%);
            color: var(--text-main);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 40px 20px;
        }

        /* Ambient background noise */
        body::before {
            content: "";
            position: absolute;
            inset: 0;
            background: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.03'/%3E%3C/svg%3E");
            pointer-events: none;
            z-index: -1;
            position: fixed;
        }

        .container {
            width: 100%;
            max-width: 600px; /* Widened for table */
            display: flex;
            flex-direction: column;
            gap: 24px;
            z-index: 1;
        }

        /* Shared Glass Panel Style */
        .glass-panel {
            background: rgba(15, 15, 15, 0.6);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-color);
            border-radius: 24px;
            padding: 40px;
            box-shadow: 0 0 0 1px rgba(0,0,0, 0.2), 0 20px 60px rgba(0,0,0,0.6);
            transition: transform 0.3s ease, border-color 0.3s ease;
        }

        .glass-panel:hover {
            border-color: var(--border-hover);
        }

        /* Typography */
        h1 {
            font-size: 32px;
            font-weight: 500;
            letter-spacing: -0.04em;
            margin-bottom: 8px;
            background: linear-gradient(180deg, #fff, #888);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-align: center;
        }

        .subtitle {
            text-align: center;
            font-size: 14px;
            color: var(--text-muted);
            margin-bottom: 40px;
            font-weight: 300;
        }

        /* Inputs & Form */
        .input-wrapper { position: relative; margin-bottom: 24px; }

        label {
            position: absolute;
            left: 16px;
            top: -10px;
            background: var(--bg-dark);
            padding: 0 6px;
            font-size: 12px;
            color: var(--text-muted);
            z-index: 2;
        }

        input {
            width: 100%;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 16px;
            font-size: 15px;
            color: #fff;
            outline: none;
            transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            font-family: 'Inter', sans-serif;
        }

        input:focus {
            border-color: #666;
            background: rgba(255, 255, 255, 0.06);
            box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.05);
        }

        button {
            position: relative;
            width: 100%;
            padding: 16px;
            border-radius: 12px;
            border: none;
            background: #fff;
            color: #000;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            overflow: hidden;
            transition: transform 0.2s;
        }

        button:hover { transform: scale(0.98); opacity: 0.9; }
        button:active { transform: scale(0.95); }

        /* Result Box */
        .result-card {
            margin-bottom: 30px;
            background: #111;
            border: 1px solid #333;
            border-radius: 12px;
            padding: 16px;
            display: flex;
            flex-direction: column;
            gap: 8px;
            position: relative;
            overflow: hidden;
        }

        .result-card::after {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; height: 1px;
            background: linear-gradient(90deg, transparent, #2a8af6, transparent);
        }

        .result-title {
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #555;
            font-weight: 700;
        }

        .result-link {
            font-family: 'JetBrains Mono', monospace;
            color: #4ade80;
            font-size: 14px;
            word-break: break-all;
        }

        /* --- TABLE STYLES (Added these for you) --- */
        .history-panel {
            padding: 0;
            overflow: hidden;
        }

        .table-wrapper {
            width: 100%;
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 14px;
            text-align: left;
        }

        thead {
            background: rgba(255, 255, 255, 0.02);
            border-bottom: 1px solid var(--border-color);
        }

        th {
            padding: 16px 24px;
            font-weight: 500;
            color: var(--text-muted);
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            white-space: nowrap;
        }

        td {
            padding: 16px 24px;
            border-bottom: 1px solid var(--border-color);
            color: var(--text-main);
            vertical-align: middle;
        }

        tr:last-child td { border-bottom: none; }
        tbody tr:hover { background: rgba(255, 255, 255, 0.03); }

        .font-mono { font-family: 'JetBrains Mono', monospace; font-size: 13px; }
        .text-accent { color: #4ade80; }
        
        .max-w-cell {
            max-width: 200px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .credits {
            margin-top: 30px;
            text-align: center;
            font-size: 12px;
            color: #444;
        }
    </style>
</head>
<body>

    <div class="container">
        
        <div class="glass-panel">
            <h1>Shorten.io</h1>
            <p class="subtitle">Minimalist URL Shortener for Professionals</p>

            <% if (locals.id) { %>
                <div class="result-card">
                    <span class="result-title">Generated Link</span>
                    <a href="http://localhost:8001/url/<%= id %>" class="result-link" target="_blank">
                        http://localhost/url/<%= id %>
                    </a>
                </div>
            <% } %>

            <form method="POST" action="/url">
                <div class="input-wrapper">
                    <input 
                        type="url" 
                        name="url" 
                        placeholder="Paste your long URL here..." 
                        required
                        autocomplete="off"
                    />
                </div>
                <button type="submit">Generate Short Link</button>
            </form>
        </div>

        <% if (locals.urls && locals.urls.length > 0) { %>
            <div class="glass-panel history-panel">
                <div class="table-wrapper">
                    <table>
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Short ID</th>
                                <th>Redirect Link</th>
                                <th>Clicks</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% urls.forEach((url, index) => { %>
                                <tr>
                                    <td><%= index + 1 %></td>
                                    
                                    <td class="font-mono text-accent">
                                        <%= url.shortId %>
                                    </td>
                                    
                                    <td class="max-w-cell">
                                        <a href="<%= url.redirectURL %>" target="_blank" style="color: #3b82f6; text-decoration: none;">
                                            <%= url.redirectURL %>
                                        </a>
                                    </td>
                                    
                                    <td style="padding-left: 32px;">
                                        <%= url.visitHistory.length %>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        <% } %>

        <div class="credits">
            Designed & Built by Shivam
        </div>
    </div>

</body>
</html>